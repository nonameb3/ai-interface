# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-your-openai-api-key-here

# Pinecone Vector Database Configuration
# Get your API key from: https://app.pinecone.io/
PINECONE_API_KEY=pcsk_your-pinecone-api-key-here
PINECONE_INDEX_NAME=portfolio-knowledge

# AI Model Configuration (Optional)
# Available models: gpt-4o, gpt-4-turbo-preview, gpt-3.5-turbo
OPENAI_MODEL=gpt-4-turbo-preview
# Available embeddings: text-embedding-3-small, text-embedding-3-large
EMBEDDING_MODEL=text-embedding-3-small

# Portfolio Configuration
# This name will appear in the UI and AI responses
NEXT_PUBLIC_PORTFOLIO_NAME=Your Name

# Admin Panel Configuration (Optional)
# Set a secure password to enable the admin panel for document management
# Leave empty to disable admin panel
ADMIN_PASSWORD=your-secure-admin-password

# CORS Configuration
# For development
ALLOWED_DOMAINS=http://localhost:3000
# For production (replace with your domain)
# ALLOWED_DOMAINS=https://yourdomain.com
# For multiple domains (comma-separated)
# ALLOWED_DOMAINS=https://yourdomain.com,https://www.yourdomain.com